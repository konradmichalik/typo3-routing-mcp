# cat=basic/enable/010; type=string; label=Bearer token env variable name:Name of the process environment variable that holds the expected bearer token for the MCP endpoint. Only the variable NAME is stored here, never the secret value. Without a configured secret, the endpoint is entirely inactive. Defaults to ROUTING_MCP_BEARER_TOKEN when left empty.
bearerTokenEnvName = ROUTING_MCP_BEARER_TOKEN

# cat=basic/enable/020; type=string; label=Endpoint path:The request path this extension listens on for MCP Streamable HTTP traffic. Must start with a leading slash. Defaults to /_mcp when left empty.
endpointPath = /_mcp
