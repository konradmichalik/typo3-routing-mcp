# cat=basic/enable/010; type=string; label=Bearer token env variable name:Name of the process environment variable that holds the expected bearer token for the /_mcp endpoint. Only the variable NAME is stored here, never the secret value. Without a configured secret, the /_mcp endpoint is entirely inactive. Defaults to ROUTING_MCP_BEARER_TOKEN when left empty.
bearerTokenEnvName = ROUTING_MCP_BEARER_TOKEN
